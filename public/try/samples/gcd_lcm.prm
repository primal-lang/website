/*
  Input: two positive integers
  Output: their Greatest Common Divisor (GCD) and Least Common Multiple (LCM)
*/

gcd(a, b) = if (b == 0) a else gcd(b, a % b)

lcm(a, b) = (a * b) / gcd(a, b)

main = [gcd(48, 18), lcm(48, 18)]