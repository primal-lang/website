/*
  Input: a list of elements
  Output: a list with the elements in reverse order
*/

reverseList(list) = if (list_isEmpty(list))
                        []
                    else
                        reverseList(list_rest(list)) + list_first(list)

main() = reverseList([1, 2, 3, 4, 5])