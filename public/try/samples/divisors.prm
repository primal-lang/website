divisors(n) = divisorsHelper(n, 1)

divisorsHelper(n, x) = if (x > n)
                           []
                       else if ((n % x) == 0)
                           x + divisorsHelper(n, x + 1)
                       else
                           divisorsHelper(n, x + 1)

main = divisors(10)