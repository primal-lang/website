/*
  Input: a list of elements
  Output: a list with the elements in reverse order
*/

listReverse(list) = if (list.isEmpty(list))
                        []
                    else
                        listReverse(list.rest(list)) + list.first(list)

main = listReverse([1, 2, 3, 4, 5])