frequency(list) = frequencyHelper(list, {})

frequencyHelper(list, result) = if (list.isEmpty(list))
                                    result
                                else if (map.containsKey(result, list.first(list)))
                                    frequencyHelper(list.tail(list), map.set(result, list.first(list), result[list.first(list)] + 1))
                                else
                                    frequencyHelper(list.tail(list), map.set(result, list.first(list), 1))

main = frequency([1, 2, 2, 3, 1, 4, 5, 2, 2, 5])