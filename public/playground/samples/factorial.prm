/*
  Input: a positive number
  Output: the factorial of that number
*/

factorial(n) = if (n == 0)
                   1
               else
                   n * factorial(n - 1)

main() = factorial(5)