/*
  Input: a string
  Output: true if the string is a palindrome, false otherwise
*/

isPalindrome(string) =
    let
        end = str_length(string) - 1
    in
        isPalindrome_helper(string, 0, end)

isPalindrome_helper(string, start, end) =
    if (start >= end)
        true
    else if (string[start] != string[end])
        false
    else
        isPalindrome_helper(string, start + 1, end - 1)

main() = isPalindrome("level")